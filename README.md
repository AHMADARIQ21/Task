Untuk menginstal paket yang diperlukan, jalankan perintah berikut di terminal:
$pip install -r requirements.txt

Setelah instalasi berhasil jalankan perintah berikut:
$python manage.py migrate
$python manage.py runserver
